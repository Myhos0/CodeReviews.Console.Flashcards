namespace Flashcards.Menus.Enums;

internal enum StackMenuOptions
{
    Select,
    Create,
    Update,
    View,
    Delete,
    Exit
}

internal enum MainMenuOptions
{
    Stack,
    StudySession,
    ReportSystem,
    Exit
}

internal enum FlashcardMenuOptions 
{
    Create,
    View,
    Update,
    Delete,
    Exit
}

internal enum SessionStudyMenuOptions 
{
   StartSession,
   ViewAllSessions,
   ViewCurrentStackSessions,
   Exit
}

internal enum ReportSystemMenuOptions 
{
    SessionPerMonth,
    AverageScorePerMonth,
    Exit
}